{
  "name": "raijinxsusanoo",
  "version": "0.1.0",
  "private": true,
  "description": "RAIJIN (premium selling) + SUSANOO (long premium) trading systems — shared infra, risk engine, Cloudflare Workers",
  "type": "module",
  "scripts": {
    "worker:dev": "npx wrangler dev --config shared/cloudflare-worker/wrangler.toml",
    "worker:deploy": "npx wrangler deploy --config shared/cloudflare-worker/wrangler.toml",
    "raijin:worker:deploy": "npx wrangler deploy --config RAIJIN/cloudflare-worker/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
